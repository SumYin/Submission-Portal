"use client"

import * as React from "react"

export type CalendarProps = { className?: string }

export function Calendar(_props: CalendarProps) {
  // Placeholder component to avoid build errors when react-day-picker isn't available.
  return null
}
